name: Get Citation Data

on:
 workflow_dispatch:
 page_build:
 schedule:
  - cron:  '0 8 * * *'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    # Google Scholar blocks Actions runner IPs, and scholarly retries until the
    # 6h job ceiling. Fail fast instead of burning 6 Actions-hours per day.
    timeout-minutes: 20
    steps:
    - uses: actions/checkout@v4
    # Pin an interpreter of our own. The system python3 on newer runner images
    # is PEP 668 "externally managed", so `pip3 install` into it is refused.
    - uses: actions/setup-python@v5
      with:
        python-version: '3.12'
    - name: Run
      run: |
        cd ./google_scholar_crawler
        python -m pip install --upgrade pip
        python -m pip install -r requirements.txt
        python main.py
        cd ./results
        git init
        git config --local user.name "${GITHUB_ACTOR}"
        git config --local user.email "${GITHUB_ACTOR}@users.noreply.github.com"
        export remote_repo="https://${GITHUB_ACTOR}:${{ secrets.GITHUB_TOKEN }}@github.com/${GITHUB_REPOSITORY}.git"
        git add *.json
        git commit -m "Updated Citation Data"
        git push "${remote_repo}" HEAD:google-scholar-stats --force
      env:
        GOOGLE_SCHOLAR_ID: ${{ secrets.GOOGLE_SCHOLAR_ID }}
        # Optional. Unset -> the runner tries Scholar directly and is blocked.
        SCRAPERAPI_KEY: ${{ secrets.SCRAPERAPI_KEY }}
